# Solid 2 train CI validation

This inert documentation marker exists only on `ci/final-solid2-train-validation` so GitHub can run the repository's unchanged pull-request CI workflow against the completed `feature/solid2-conformance-train` code without modifying the integration branch or `dev`.
