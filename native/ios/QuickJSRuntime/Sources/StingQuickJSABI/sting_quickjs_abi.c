#include "sting_quickjs_android.h"

/*
 * The implementation symbols are supplied by the generated official-QuickJS
 * + Sting/Zig static library. This translation unit gives SwiftPM a normal C
 * target that imports the ABI declarations without moving runtime semantics
 * out of Zig.
 */
