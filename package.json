{
  "name": "stingjs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@12.0.2",
  "workspaces": [
    "packages/core",
    "packages/solid",
    "packages/native",
    "packages/modules/*",
    "examples/*",
    "benchmarks/sting-benchmark",
    "benchmarks/solid2-conformance"
  ],
  "scripts": {
    "benchmark:results": "node benchmarks/results/tool.mjs",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present && node --test benchmarks/results/tool.test.mjs",
    "test:ios-runtime": "bash scripts/test-ios-runtime.sh",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "vitest": "4.1.0"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "license": "MIT"
}
